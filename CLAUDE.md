# RecallOS Flutter — Claude Instructions

## Manual Instructions
When giving the user steps they must perform manually (Supabase SQL, native config, key generation, dashboard settings, etc.), always write them as numbered, detailed steps. Include the exact commands, where to navigate, what to paste, and what to expect as output. Never summarize manual steps into a single sentence.
